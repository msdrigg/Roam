.DS_Store
Roam.xcodeproj/project.xcworkspace/xcuserdata
Roam.xcodeproj/xcuserdata
Archives/
Secrets.xcconfig
# Intermediate SVG/PNG from AppIcons/generate-alternates.py. The committed
# artifacts are the .appiconset / .imageset assets it writes into
# Shared/AppIcon.xcassets; these are regenerable.
AppIcons/generated/

# Local SPM package build artifacts
LocalPackages/**/.build/

# Crash payloads pulled from the support threads. They carry install ids, LAN
# addresses and thread contents, and they are re-downloadable from the backend
# (scripts/roam_crashes.py).
crash-analysis/

# Xcode build output from command-line `xcodebuild -derivedDataPath`/SPM builds.
build/
LocalPackages/**/build/
